#include "main.h"
#include <string.h>

/**
 * _strlen - This is the prototype function to return the lenght of a string
 *
 * @s: This is the parameter for the function
 * Return: This will return 0 for success
 */

int _strlen(char *s)
{
	return (strlen(s));
}
