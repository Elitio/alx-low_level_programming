#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - this is the code that runs first
 *
 * Return: the return value is 0 for success
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}
