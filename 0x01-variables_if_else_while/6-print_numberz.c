#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - this is the code that runs first
 *
 * Return: the return value is 0 for success
 */

int main(void)
{
	int n = 0;

	if (n < 10)
	{
		printf("%d", n);
		n++;
	}
	putchar('\n');
	return (0);
}
