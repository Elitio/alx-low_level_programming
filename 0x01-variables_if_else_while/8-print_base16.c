#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - the ifirst code that runs
 *
 * Return: this returns 0 for no errors
 */

int main(void)
{
	char n;
	char m;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (m = 97; n < 103; n++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
