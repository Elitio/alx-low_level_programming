#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - this is the part of the code that executes firts
 *
 * Return: the return value is 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
